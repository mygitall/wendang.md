---
description: NeoVision 设计 Token —— 颜色、字体、间距、圆角、动效
globs: "**/*.{html,css,js,tsx,jsx,vue,svelte}"
alwaysApply: true
version: 1
---

# NeoVision Design Tokens

## 色彩 · CSS 变量（禁止硬编码）

所有颜色通过 CSS 自定义属性驱动，定义在 `:root`（暗色）和 `[data-theme="light"]`（浅色）。

```
--bg         背景         dark:#08080b   light:#f5f5f7
--surface    卡片底色     dark:rgba(255,255,255,0.025)  light:rgba(0,0,0,0.02)
--border     边框         dark:rgba(255,255,255,0.06)   light:rgba(0,0,0,0.06)
--border-h   边框 hover   dark:rgba(255,255,255,0.10)   light:rgba(0,0,0,0.10)
--text       主文字       dark:#e4e4eb   light:#1a1a1f
--text-2     次要文字     dark:#8b8b96   light:#6b6b73
--text-3     辅助文字     dark:#5c5c66   light:#9b9ba3
--accent     主强调色     dark:#7b8cff   light:#5b6cf0
--accent-2   次强调色     dark:#c49bff   light:#8b5cf0
--nav-bg     导航背景     dark:rgba(8,8,11,0.7)  light:rgba(245,245,247,0.7)
--glow-a     光晕A        dark:rgba(120,100,255,0.08)  light:rgba(120,100,255,0.04)
--glow-b     光晕B        dark:rgba(180,140,255,0.06)  light:rgba(180,140,255,0.03)
--glow-c     光晕C        dark:rgba(100,140,255,0.05)  light:rgba(100,140,255,0.02)
--radius     全局圆角      16px
```

**规则：**
- 写任何颜色值必须用 `var(--xxx)`，绝不允许 `#xxxxxx` 或 `rgba()` 硬编码
- 每新增一个颜色变量，必须同时在 `:root` 和 `[data-theme="light"]` 下定义
- 语义色 `warm: #e0a870`、`green: #6ecf8a` 用于标签 / dot 装饰，不需要暗亮双套

## 字体

```
font-family: -apple-system, BlinkMacSystemFont,
  "SF Pro Display", "SF Pro Text",
  "PingFang SC", "Helvetica Neue", sans-serif;
```

中英文混排必须包含 PingFang SC。中文内容页默认使用此字体栈。

## 字重规范（Apple 式精细梯度）

| 用途 | 字重 | CSS |
|------|------|-----|
| Hero 标题细体 | 300 | `font-weight:300` |
| 大段引导文字 | 380 | `font-weight:380` |
| 正文 | 400 | `font-weight:400` |
| 导航链接 | 450 | `font-weight:450` |
| Section 标题 | 500 | `font-weight:500` |
| 新闻标题 | 530 | `font-weight:530` |
| 卡片标题/标签/CTA | 550 | `font-weight:550` |
| 强调数据/加粗 | 600 | `font-weight:600` |
| Logo | 650 | `font-weight:650` |

**禁止使用 700 / 800 / 900 字重。**

## 字间距梯度

| 层级 | letter-spacing |
|------|---------------|
| Hero 大标题 | `-2px` |
| Section 标题 | `-0.6px` |
| 卡片/新闻标题 | `-0.3px` |
| 导航链接 | `-0.1px` |
| 正文 | `-0.2px` |
| 标签 uppercase（eyebrow） | `+0.8px ~ +1.5px` |

## 行高

- 标题类：`1.1 ~ 1.4`
- 正文类：`1.6 ~ 1.7`

## 圆角

- 卡片：`var(--radius)` → 16px
- CTA 按钮：`100px`（胶囊）
- 头像 / 圆形按钮：`50%`

## 动效节奏

| 对象 | 属性 | 时长 |
|------|------|------|
| 主题切换 | background, color | 0.4s |
| 卡片 hover | border-color, background | 0.3s |
| 链接 hover | color | 0.2s |

主题切换必须加 transition 到 body / .card / .news-card / .nav / .footer。

## 边框铁律

- 所有分割线 / 卡片边框统一：**0.5px solid var(--border)**
- Hover 态：**0.5px solid var(--border-h)**
- 禁止用 box-shadow 区分卡片层次
- 禁止边框超过 0.5px
